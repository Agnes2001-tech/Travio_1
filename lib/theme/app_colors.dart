import 'package:flutter/material.dart';

class AppColors {
  // Obsidian Palette
  static const obsidian = Color(0xFF0A0A0B); // Deep space background
  static const obsidianSecondary = Color(0xFF161618); // Elevated cards
  static const obsidianTertiary = Color(0xFF222224); // Subtle accents
  
  // Brand & Accent
  static const primary = Color(0xFF6366F1); // Indigo/Purple accent
  static const primaryLight = Color(0xFF818CF8);
  
  // Semantic Colors
  static const textPrimary = Color(0xFFF9FAFB);
  static const textSecondary = Color(0xFF9CA3AF);
  static const textGrey = Color(0xFF6B7280);
  
  // Functional Colors
  static const star = Color(0xFFFBBF24);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  
  // Glassmorphism components
  static const glassBorder = Color(0x33FFFFFF);
  static const glassFill = Color(0x0DFFFFFF);
}